spring:
  datasource:
    url: jdbc:mysql://localhost:3306/car_rental?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: validate
    show-sql: true
